apiVersion: v1
kind: Service
metadata:
  name: operator-portal
  namespace: dnd-notes-platform
  labels:
    app.kubernetes.io/name: operator-portal
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: operator-portal
  ports:
    - port: 8080
      targetPort: 8080
      name: http
