apiVersion: apps/v1
kind: Deployment
metadata:
  name: operator-portal
  namespace: dnd-notes-platform
  labels:
    app.kubernetes.io/name: operator-portal
    app.kubernetes.io/part-of: dnd-notes
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: operator-portal
  template:
    metadata:
      labels:
        app.kubernetes.io/name: operator-portal
    spec:
      containers:
        - name: portal
          image: ghcr.io/daydream-software/dnd-notes-operator-portal:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: operator-portal-config
