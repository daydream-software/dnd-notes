apiVersion: v1
kind: Service
metadata:
  name: customer-portal
  namespace: dnd-notes-platform
  labels:
    app.kubernetes.io/name: customer-portal
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: customer-portal
  ports:
    - port: 8080
      targetPort: 8080
      name: http
