# PR #72 CI Fix Batch Session Log

**Timestamp:** 2026-04-21T20:32:58Z

## Context

PR #72 (per-tenant Postgres credentials hardening) was in second review wave with commit `f85831b`. After review closure, Data identified and fixed focused CI regressions:

- **Root cause 1:** pg-mem tests fell into least-privilege path incorrectly
- **Root cause 2:** tenant runtime role provisioning password form Postgres rejected in smoke path

## Work Summary

**Agent:** Data

**Commits:**
- `f033b69`: Fixed CI breakage by addressing pg-mem test fixture path and tenant runtime role credential rejection

**Validation:**
- `npm run lint --workspace apps/api` — **pass**
- `npm test --workspace apps/api` — **pass**
- `npm run build --workspace apps/api` — **pass**
- `npm run lint --workspace apps/control-plane` — **pass**
- `npm test --workspace apps/control-plane` — **pass**
- `npm run build --workspace apps/control-plane` — **pass**

**Outcome:**
- Focused regression coverage added (pg-mem test isolation, role credential validation)
- PR #72 now has fresh check cycle running on main after push
- Still awaits fresh review from team

## Key Learnings

- Postgres least-privilege role assignment requires strict credential form validation in provisioning paths
- pg-mem integration tests must not leak into runtime role paths during credential setup
- Smoke test password validation is a critical gate for prod-like role provisioning
