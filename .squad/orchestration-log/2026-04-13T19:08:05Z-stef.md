# Orchestration Log: Stef (Frontend Dev)

**Date:** 2026-04-13T19:08:05Z  
**Agent:** Stef (Frontend Dev)  
**Topic:** Phase 2 Implementation — Editor & Inline References

## Inspection Summary

Stef inspected the frontend editor flow in `apps/web/src/App.tsx` and reviewed the phase 2 recommendation document.

### Key Findings

1. **Markdown Sanitization (Excerpts)** — ✅ Ready
   - Current `excerpt()` function at line 331 uses `markdownToPlainText()` from `note-formatting.tsx`
   - Already strips markdown syntax, collapses whitespace, limits to 112 chars
   - HTML tag stripping via `/<[^>]+>/g` regex is solid
   - No changes needed for Phase 2

2. **Dual-Mode Editor** — 🔴 Requires implementation
   - Current: plain `<textarea>` with stacked preview below
   - Phase 2a: Add `showPreview` toggle to hide/show preview (1–2 days)
   - Phase 2b: Integrate Lexical editor with markdown import/export (4–5 days)
   - Phase 2c: Add inline reference nodes with picker UI (2–3 days)

3. **Inline References** — 🟡 Designer + implementation
   - Syntax: `![[noteId|label]]` stored directly in markdown body
   - No API contract change for Phase 2 (references stay embedded in body)
   - Backlinks computed client-side by parsing body for reference patterns
   - Phase 3 adds structured extraction to backend

### Recommendation

Proceed with phased editor implementation:
- **Phase 2a:** Extract mode toggle to a reusable NoteEditor component wrapper
- **Phase 2b:** Introduce Lexical with markdown-first approach (no format conversion)
- **Phase 2c:** Add custom NoteRefNode for inline reference syntax + picker UI

**No blocking dependencies** — can start Phase 2a immediately.

## Deliverables for Phase 2

### Frontend
- `NoteEditor.tsx` (new): Lexical composer wrapper with markdown config
- `App.tsx`: Remove stacked preview, integrate mode toggle and NoteEditor
- `note-formatting.tsx`: Enhanced excerpt sanitization for reference syntax (optional)

### Dependencies
- `lexical` + `@lexical/markdown` packages

### Acceptance Criteria
- Mode toggle hides/shows preview without draft loss
- Editor preserves markdown on save/load (byte-for-byte)
- Raw markdown view available
- Reference picker inserts syntax correctly
- All 26 baseline tests pass + new regression coverage
- No performance regression

## Timeline

| Phase | Owner | Est. | Status |
|-------|-------|------|--------|
| 2a (toggle) | Stef or Copilot | 1–2d | Ready to start |
| 2b (Lexical) | Stef | 4–5d | Post-2a blocker |
| 2c (references) | Stef + Data | 2–3d | Post-2b blocker |

---

**Outcome:** Stef is ready to execute Phase 2a. Lexical architecture review recommended before Phase 2b coding begins.
