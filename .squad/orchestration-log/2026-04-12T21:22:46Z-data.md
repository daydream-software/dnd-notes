# Data — Issue #27 Backend Revision Round Complete

**Date:** 2026-04-12  
**Status:** COMPLETE

## Work Summary

Data completed the backend rescue pass for issue #27 session-browsing API, addressing all four regressions Chunk identified:

1. **Route shadowing fix:** Moved `/api/notes/sessions*` routes ahead of the catch-all `/:noteId` route in Express so `"sessions"` is no longer consumed as a note ID.
2. **Double-decode fix:** Removed manual `decodeURIComponent()` from session detail endpoint because Express already hands us a decoded `sessionId`.
3. **Auth regression fix:** Switched both session-browsing routes from `resolveOwnedCampaign()` to `resolveAccessibleCampaign()`, matching the sibling `/api/notes` endpoint's auth pattern and granting linked collaborators proper access.
4. **Implementation alignment:** Kept the existing `SessionsResponse` and `NotesResponse` TypeScript contracts in sync, confirmed no additional backend contract changes needed beyond the auth/routing corrections.

## Validation

- `npm run lint --workspace apps/api` — PASSED
- `npm test --workspace apps/api` — PASSED  
- `npm run build --workspace apps/api` — PASSED

## Frontend Handoff Note

Session detail URLs must be encoded with `encodeURIComponent(sessionName)` exactly once at build time. The endpoints are ship-safe for any authenticated linked collaborator; no additional scoping is needed.

## Decision Captured

Decision saved to `.squad/decisions/inbox/data-revise-27.md` for merge into canonical decisions log.

## Next Work

Backend is complete and ship-safe. Stef to begin thin session-browsing UI slice on `SessionsResponse` contract.
