# Orchestration Log: Data (Backend Dev)

**Timestamp:** 2026-04-18T22:36:25Z  
**Agent:** Data  
**Status:** Complete

## Summary

Data analyzed issue #53 (Control-plane skeleton) and determined it can start immediately in parallel with Phase 0 (#52, #43, #46). Designed control-plane architecture: single-replica SQLite for Phase 1, 7-state tenant lifecycle model, thin internal API skeleton, and documented migration path to Postgres post-Phase-1.

## Decisions Made

1. **Issue #53 — GO, start immediately, parallel with #52**
   - New service: `apps/control-plane/` (Node.js + Express)
   - Database: Single-replica SQLite (Phase 1), documented Postgres migration path (Phase 2)
   - Tenant registry schema: 7-state lifecycle model (provisioning, ready, maintenance, upgrading, restoring, failed, deprovisioned)
   - Internal API: 4 core endpoints (`POST /internal/tenants`, `GET /internal/tenants`, `PATCH /internal/tenants/:id`, backup registry thin slice)
   - Audit table: State transitions for fleet visibility
   - Effort: 12–16 hours

2. **Architecture choices:**
   - Control plane uses SQLite (not Postgres) in Phase 1 — single-replica, zero-scaling, simpler local dev
   - Separate from tenant databases (Postgres per-instance) — different lifecycle, different concerns
   - State machine enforces valid transitions; tenant registry holds observed + desired state
   - K8s API is coordination layer; control plane reads K8s, acts on observed state

3. **No code dependencies on #52:**
   - Control plane and tenant app are separate services
   - Can develop/test in isolation; integration in #54 (provisioning orchestrator)

## Blockers

None. Self-contained work; no architectural gaps.
