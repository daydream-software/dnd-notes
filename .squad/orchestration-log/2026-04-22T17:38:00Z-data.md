# 2026-04-22T17:38:00Z — Data

**Status:** ✅ Complete

## Role
Backend Dev

## Mission
Harden the control-plane rollout failure contract for versioned provision requests reused by the operator portal.

## Outcome
Ready-tenant rolling updates now return stable typed `400`/`409`/`500` responses for unsupported targets, concurrent/disallowed rollouts, and mid-flight rollout failures, while first-time provisioning keeps the older generic error shape.

## Changes
- `apps/control-plane/src/provisioning.ts`: classified rollout guardrails with typed failures
- `apps/control-plane/src/app.ts`: translated rollout failures into stable HTTP status/code/detail responses
- `apps/control-plane/test/provisioning.test.ts`: covered service-side rollout guards
- `apps/control-plane/test/app.test.ts`: covered HTTP mapping for rollout failures
- `apps/control-plane/README.md`: documented operator-facing failure codes

## Decision Authored
- **2026-04-22: Data — Issue #68 rollout failure hardening** — keep the provision route as the canonical write path, but make ready-tenant rolling-update failures explicit and operator-friendly.

## Validation
- Focused control-plane tests passed.
- Operator-portal validation reran successfully against the hardened contract.

## Notes
- Shared worktree was already dirty with unrelated #68 changes, so no code commit was cut for this backend slice.
