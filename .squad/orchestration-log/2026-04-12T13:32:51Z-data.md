# Agent: Data — 2026-04-12T13:32:51Z

**Role:** Backend Dev  
**Task:** Fix merged PR runtime regression causing `SqliteError: no such column: notes.created_by_membership_id` on dev startup  
**Outcome:** FIXED

## Summary

Added backend compatibility logic to upgrade older SQLite `notes` tables in place before note queries are prepared, preserving existing local data and allowing legacy notes to expose null attribution.

## Work Completed

1. **Root cause:** PR #21 added nullable `created_by_membership_id` and `last_edited_by_membership_id` columns to `notes` table, but existing local dev databases still had the older schema. `apps/api/src/note-store.ts` prepares statements during startup before schema bootstrap, causing immediate failure.

2. **Fix:** Added schema introspection and in-place column upgrades to `createNoteStore()` in `apps/api/src/note-store.ts`:
   - Detects if attribution columns are missing
   - Adds them as nullable if needed
   - Ensures backward compatibility for legacy notes

3. **Tests:** Added regression coverage in `apps/api/test/app.test.ts`:
   - Creates pre-attribution `notes` table schema
   - Inserts legacy note data
   - Reopens store through `createNoteStore()`
   - Verifies legacy notes load with null attribution fields

4. **Verification:** Confirmed lint, test, and build pass; `npm run dev` startup clean.

## Files Modified

- `apps/api/src/note-store.ts`
- `apps/api/test/app.test.ts`
- `README.md`
- `.squad/agents/data/history.md`
- `.squad/skills/sqlite-startup-compatibility/SKILL.md`

## Status

✅ Revision not required. Ready for next phase.
