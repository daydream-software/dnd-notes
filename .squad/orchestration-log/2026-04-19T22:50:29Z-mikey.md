# Orchestration: Mikey — Issue #58 Decisions (LOCKED)

**Agent:** Mikey (Lead)  
**Task:** Lock three architectural decisions for NoteStore Postgres adapter  
**Outcome:** 🔒 LOCKED — Ready for Data implementation  
**Timestamp:** 2026-04-19T22:50:29Z

## Summary

Mikey locked three load-bearing decisions for Issue #58:

### Decision 1: Transaction Isolation Level
- **Choice:** `SERIALIZABLE` on Postgres
- **Rationale:** Matches SQLite contract; prevents dirty reads, phantom reads
- **Implementation:** Set via connection string or `SET TRANSACTION ISOLATION LEVEL SERIALIZABLE`
- **Retry logic:** Exponential backoff on serialization conflicts (max 3 attempts)

### Decision 2: Connection Pool Defaults
- **Settings:** minConnections=2, maxConnections=10, idleTimeout=30s, statementTimeout=30s
- **Rationale:** Safe rolling updates, graceful shutdown, prevent connection leak
- **Phase 1 adjustment:** Revisit when multi-tenant load models exist

### Decision 3: SQLite Fallback Selection Rule
- **Logic:** `DATABASE_URL` env var → Postgres; missing → SQLite fallback
- **Rationale:** Standard Heroku pattern; prevents accidental production SQLite
- **Safeguard:** Warn if Postgres selected but unreachable (fail fast)

## Test Coverage (Chunk's Gate)

All 26+ API tests must pass against both Postgres (CI) and SQLite (local).
Concurrency tests validate isolation level, pool saturation, reference sync atomicity.

Full decisions: `.squad/decisions/inbox/mikey-issue-58-decisions.md`
