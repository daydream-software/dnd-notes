# Orchestration: Data (Backend Dev) — Phase 2 Backend/Platform-Security Start Order

**Date:** 2026-04-21T19:37:42Z  
**Agent:** Data (Backend Dev)  
**Status:** Backend start strategy defined; #69 thin slice prioritized

## Output

**Document:** `.squad/decisions/inbox/data-phase-2-backend-plan.md` (4.6 KB)

## Decision

Start Phase 2 with **#69 per-tenant Postgres credentials** before full #56 OIDC wiring or #40 restore orchestration.

### Thin Safe Slice (#69 first-pass)
1. Control plane creates tenant-scoped runtime role + password
2. Control plane pre-seeds tenant database schema with elevated credentials before first pod start
3. Tenant pod receives only tenant-scoped runtime `DATABASE_URL`
4. Control plane records credential lifecycle metadata/audit events

### #56 Prep (no full wiring yet)
- Auth-provider seam in `route-support.ts` (decouple identity provider)
- Stable authenticated principal shape definition
- Local login/register isolation
- Deferred: full OIDC integration

### #40 Prep (no full operator workflow yet)
- Process-level maintenance / write-block hook
- Graceful restore flow (maintenance → restore → reopen)
- Centralized write-block behavior across routes
- Deferred: full operator notification

## Risks & Mitigation

1. Bootstrap privilege mismatch → move schema DDL to pre-initialization phase
2. Existing tenant credential rotation → audit metadata tracking
3. Test harness gap → Postgres-backed integration tests for privilege isolation
4. Restore interaction → #40 adapts to per-tenant credential model

## Next Steps

- Begin #69 implementation (provisioning.ts, role creation, schema initialization)
- Identify schema pre-initialization seam
- Plan audit metadata table/structure
